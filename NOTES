Java :- 
        
        It is a popular programming language, created in 1995.
        It is owned by Oracle, and more than 3 billion devices run Java.
        It is used for:
            Mobile applications (specially Android apps)
            Desktop applications
            Web applications
            Web servers and application servers
            Games
            Database connection
            And much, much more!

Package :- All related code stored in packages so that it can be eaily accessed.
        e.g. :- in the library all books are stored in different sections so that it can be eaily accessed.
                like all history books in one section and all science books in one section.
                syntax:- Package com.packagename;
                

Comments in Java :-

        .Single line comments :- 
                Single-line comments start with two forward slashes (//).
                Any text between // and the end of the line is ignored by Java.

        . Multiline Comments:- 
                Multi-line comments start with /* and ends with */.
                Any text between /* and */ will be ignored by Java.

Java Variables:-

        Variables are containers for storing data values.
        In Java, there are different types of variables, for example:
            String - stores text, such as "Hello". String values are surrounded by double quotes
            int - stores integers (whole numbers), without decimals, such as 123 or -123
            float - stores floating point numbers, with decimals, such as 19.99 or -19.99
            char - stores single characters, such as 'a' or 'B'. Char values are surrounded by single quotes
            boolean - stores values with two states: true or false


JVM, JRE, JDK :-
        (Java Virtual Machine - Specification)
        (Java Runtime Environment - It is the implementation of JVM)
        (Java Development Kit - Kit that contaion Development tools, JRE)

TYPECASTING :-
        Converting one datatype to another datatype.
        e.g. int a=5;
                double b=a; //implicit typecasting
                double b=(double)a; //explicit typecasting
                int a=(int)5.6f; //explicit typecasting
                


OPERATORS :- 

        Ranks:- 
                1. Brackets
                2. Divide / Multiply :- system run according to the position of it in the code.
                3. Addition / Subtraction :- system run according to the position of it in the code.


TAKING INPUT AND OUTPUT
        OUTPUT :- 
                System.out.println("Hello World");
                System.out.println(3+3);
                System.out.println(15*6);
        INPUT :-
                Scanner scn = new Scanner(System.in);
                int n=scn.nextInt();
                e.g.
                import java.util.Scanner;
                class studnet {
                  public static void main(String[] args) {
                    Scanner scn = new Scanner(System.in);
                        int n=scn.nextInt();
                        String name=scn.nextLine();
                        System.out.println("hii"+n);    
                }
        }
 IF AND ELSE 
        if(condition){
                //code
        }
        else{
                //code
        }
        e.g.
        import java.util.Scanner;
        class studnet {
          public static void main(String[] args) {
            Scanner scn = new Scanner(System.in);
                int n=scn.nextInt();
                if(n%2==0){
                        System.out.println("even");
                }
                else{
                        System.out.println("odd");
                }
        }
        }
        else if(condition){
                //code
        }
        else{
                //code
        }
        e.g.
        import java.util.Scanner;
        class studnet {
          public static void main(String[] args) {
            Scanner scn = new Scanner(System.in);
                int n=scn.nextInt();
                if(n%2==0){
                        System.out.println("even");
                }
                else if(n%2==1){
                        System.out.println("odd");
                }
                else{
                        System.out.println("invalid");
                }
        }
        }
        e.g.
        import java.util.Scanner;
        class studnet {
          public static void main(String[] args) {
            Scanner scn = new Scanner(System.in);
                int n=scn.nextInt();
                if(n%2==0){
                        System.out.println("even");
                }
                else if(n%2==1){
                        System.out.println("odd");
                }
                else if(n%2==2){
                        System.out.println("even");
                }
                else if(n%2==3){
                        System.out.println("odd");
                }
                else{
                        System.out.println("invalid");
                }
        }
        }
        e.g.
        import java.util.Scanner;
        class studnet {
          public static void main(String[] args) {
            Scanner scn = new Scanner(System.in);
                int n=scn.nextInt();
                if(n%2==0){
                        System.out.println("even");
                }
                else if(n%2==1){
                        System.out.println("odd");
                }
                else if(n%2==2){
                        System.out.println("even");
                }
                else if(n%2==3){
                        System.out.println("odd");
                }
                else if(n%2==4){
                        System.out.println("even");
                }
                else if(n%2==5){
                        System.out.println("odd");
                }
                else{
                        System.out.println("invalid");
                }
        }
        }


FOR LOOP :
        for(int i=0;i<10;i++){
                //code
        }
        e.g.
        import java.util.Scanner;
        class studnet {
          public static void main(String[] args) {
            Scanner scn = new Scanner(System.in);
                for(int i=0;i<10;i++){
                        System.out.println(i);
                }
        }
        }

WHILE LOOP :
        while(condition){
                //code
        }
        e.g.
        import java.util.Scanner;
        class studnet {
          public static void main(String[] args) {
            Scanner scn = new Scanner(System.in);
                int i=0;
                while(i<10){
                        System.out.println(i);
                        i++;
                }
        }
        }

DO WHILE LOOP :
        do{
                //code
        }while(condition);
        e.g.
        import java.util.Scanner;
        class studnet {
          public static void main(String[] args) {
            Scanner scn = new Scanner(System.in);
                int i=0;
                do{
                        System.out.println(i);
                        i++;
                }while(i<10);
        }
        }

SWITCH CASE :
        switch(expression){
                case 1:
                        //code
                        break;
                case 2:
                        //code
                        break;
                default:
                        //code
        }
        e.g.
        import java.util.Scanner;
        class studnet {
          public static void main(String[] args) {
            Scanner scn = new Scanner(System.in);
                int i=0;
                switch(i){
                        case 1:
                                System.out.println("one");
                                break;
                        case 2:
                                System.out.println("two");
                                break;
                        default:
                                System.out.println("invalid");
                }
        }
        }

ARRAYS :
        int[] arr = new int[5];
        int[] arr = {1,2,3,4,5};
        e.g.
        import java.util.Scanner;
        class studnet {
          public static void main(String[] args) {
            Scanner scn = new Scanner(System.in);
                int[] arr = {1,2,3,4,5};
                for(int i=0;i<arr.length;i++){
                        System.out.println(arr[i]);
                }
        }
        }

FUNCTIONS :
        It is a block of code that performs a specific task.
        It is executed when it is called.
        It is used to break large programs into smaller ones.
        It makes the code reusable.
        It makes the code easier to understand.
        It is used to avoid repetition in the program.
        It is used to eliminate the redundant code.
        It is used to improve the readability of the program.
        It is used to reduce the length of the program.\
        It is used to reduce the complexity of the program.

FUNCTIONS WITHOUT PARAMETERS :


        public static void name(){
                //code
        }
        e.g.
        import java.util.Scanner;
        class studnet {
          public static void main(String[] args) {
            Scanner scn = new Scanner(System.in);
                name();
        }
        public static void name(){
                System.out.println("hii");
        }
}

FUNCTIONS WITH PARAMETERS :
        public static void name(int a){
                //code
        }
        e.g.
        import java.util.Scanner;
        class studnet {
          public static void main(String[] args) {
            Scanner scn = new Scanner(System.in);
                name(5);
        }
        public static void name(int a){
                System.out.println(a);
        }
}

FUNCTIONS WITH RETURN TYPE :
        public static int name(int a){
                //code
                return a;
        }
        e.g.
        import java.util.Scanner;
        class studnet {
          public static void main(String[] args) {
            Scanner scn = new Scanner(System.in);
                int a=name(5);
                System.out.println(a);
        }
        public static int name(int a){
                return a;
        }
}

FUNCTIONS WITH PARAMETERS AND RETURN TYPE :
        public static int name(int a){
                //code
                return a;
        }
        e.g.
        import java.util.Scanner;
        class studnet {
          public static void main(String[] args) {
            Scanner scn = new Scanner(System.in);
                int a=name(5);
                System.out.println(a);
        }
        public static int name(int a){
                return a;
        }
}
 