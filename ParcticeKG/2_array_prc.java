package ParcticeKG;
import java.util.*;

// 1. WAP to find sum and average of an array
/* 

class array_prc{
    public static void main(String[] args) {
        System.out.println("Welcome to array sum and average");
        int [] numArray =ArrayUtility.inputArray(); // calling the another class using this 
        // here ArrayUtility is the new class where we define the input array class
        long sum =sum(numArray);
        // int average =average(numArray);
        double average =average(numArray);
        System.out.println("The sum of the array is " + sum);
        System.out.println("The average of the array is " + average);
    }

    public static long sum(int [] numArray) {
        long sum=0;
        int i=0;
        while (i<numArray.length) {
            sum+=numArray[i];
            i++;            
        }
        return sum;
    }

    public static double average(int[] numArray) {
    //     long sum=sum(numArray); for integer answwer
    //    return (int)(sum/numArray.length);
    double sum=sum(numArray);
    return (sum/numArray.length);
    }

}

*/

// 